# LangChain4j 总体学习指导（总览）

> 本文件是站在**更高角度**审视 LangChain4j 的总览文档。帮助你建立"这个框架到底能干什么、各个组件怎么协作、在企业里能落地成什么"的整体心智模型。
> 配套各模块的 `学习指导.md`（按优先级排了每个类的学习顺序），本文件回答"为什么"和"整体长什么样"。

---

## 一、LangChain4j 是什么

**一句话**：LangChain4j 是给 **Java 生态** 的 LangChain——一个把"大语言模型（LLM）"封装成正经 Java 库的框架。

它解决的痛点：
- **厂商众多**（OpenAI、Azure、Google Gemini、Claude、Ollama、通义……），各自 API 不同，LangChain4j 用**统一接口**把它抽象掉，你换厂商只改一行配置。
- **LLM 输出是自由文本**，不可靠，LangChain4j 提供**结构化输出**，直接转成枚举/POJO/List。
- **LLM 无记忆、无外部工具、无私有知识**，LangChain4j 提供**记忆、工具调用（Function Calling）、RAG（检索增强）** 三板斧补齐。

一句话总结它的哲学：**像写普通 Java 代码一样写 AI 功能，业务代码不感知背后是 LLM。**

---

## 二、LangChain4j 的能力版图

```
┌─────────────────────────────────────────────────────────────────┐
│                    LangChain4j 能力全景                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ 模型接入     │  │ 提示词引擎   │  │   记忆       │              │
│  │ Chat/流式/  │  │ 模板+变量    │  │ 内存/按用户/ │              │
│  │ Embedding/  │  │ 结构化提示词 │  │ 持久化       │              │
│  │ Image/Audio │  └─────────────┘  └─────────────┘              │
│  │ 多厂商统一   │                                                 │
│  └─────────────┘                                                 │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ AI 服务     │  │ 工具调用     │  │   RAG 检索   │              │
│  │ AiService   │  │ @Tool       │  │ 切块/向量化/ │              │
│  │ 声明式接口   │  │ Function    │  │ 检索/重排    │              │
│  │ 结构化输出   │  │ Calling     │  └─────────────┘              │
│  └─────────────┘  └─────────────┘                               │
│                                                                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Agent 编排  │  │ 可观测性     │  │ 集成生态     │              │
│  │ 顺序/循环/  │  │ Listener 监听│  │ Spring Boot │              │
│  │ 并行/Super  │  │ 日志/成本    │  │ Quarkus/Helidon│            │
│  │ visor/人机  │  └─────────────┘  │ 向量库/数据库 │              │
│  └─────────────┘                   └─────────────┘              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

八大能力，也是你学习的主线：

| 能力 | 说明 | 对应学习模块 |
|------|------|------|
| 1. 模型接入 | 聊天/流式/向量/图/音，多厂商统一接口 | tutorials、open-ai-examples |
| 2. 提示词引擎 | 模板 + 变量 + 结构化提示词 | tutorials |
| 3. 记忆 | 多轮上下文、按用户隔离、持久化 | tutorials、other-examples |
| 4. AI 服务 | **最核心**：声明式接口 + 自动结构化输出 | tutorials、spring-boot-example |
| 5. 工具调用 | 让 LLM 调用你的 Java 方法 | tutorials、agentic-tutorial |
| 6. RAG 检索 | 私有知识库问答，增强、重排、多源 | rag-examples、pgvector-example |
| 7. Agent 编排 | 多个 Agent 组成工作流 | agentic-tutorial、mcp-example |
| 8. 可观测性/集成 | 监听、成本、日志；Spring 等框架 | open-ai-examples、spring-boot-example |

---

## 三、LangChain4j 架构关系（核心概念分层）

```
                         ┌─────────────────────────────┐
                         │       业务代码 / Controller   │
                         │  (Spring MVC / REST 层)       │
                         └──────────────┬──────────────┘
                                        │ 调用
                    ┌───────────────────▼───────────────────┐
                    │          AI 服务 (AiService)           │   ← 工程化核心
                    │  你只需定义「接口 + 注解」，框架生成实现  │
                    │  @SystemMessage / @UserMessage / @Tool │
                    │  自动把参数拼提示词、自动解析返回类型     │
                    └───┬──────────┬──────────┬─────────────┘
                        │          │          │
           ┌────────────▼──┐  ┌────▼─────┐  ┌▼───────────────┐
           │  聊天模型      │  │  记忆     │  │   工具          │
           │  ChatModel    │  │ ChatMemory│  │ @Tool 方法      │
           └──────┬────────┘  └──────────┘  └──────┬─────────┘
                  │                                │
                  │   ┌────────────────────────────┘ (工具可选)
                  │   │
          ┌───────▼───▼─────────────┐
          │      模型接口统一层        │
          │  ChatModel / EmbeddingModel│
          │  ImageModel / AudioModel  │
          └───────┬─────────┬────────┘
                  │         │
   ┌──────────────▼──┐   ┌─▼──────────────────┐
   │   向量化模型      │   │   检索增强 RAG      │
   │ EmbeddingModel  │   │ ContentRetriever   │
   └──────┬──────────┘   │ RetrievalAugmentor │
          │              └───┬───────────────┘
          │  将文本变成向量     │  检索出相关知识
   ┌──────▼──────────┐   ┌───▼───────────────┐
   │   向量存储       │   │  文档处理          │
   │ EmbeddingStore  │   │ DocumentSplitter  │
   │ (pgvector/Redis │   │ DocumentLoader     │
   │  /内存/向量库)   │   └───────────────────┘
   └─────────────────┘
```

**关键：AiService 是"心脏"。** 它把上面所有零件（模型、记忆、工具、检索器）用接口+注解的方式"拼"起来，业务代码只跟接口打交道。

---

## 四、LangChain4j 重要类及它们的关系

### 1. 模型层（你最少要认识的接口）

| 类/接口 | 作用 | 关系 |
|---------|------|------|
| `ChatModel` | 同步聊天模型，`chat()` 返回完整回答 | 最常用，任何厂商实现它 |
| `StreamingChatModel` | 流式聊天，`chat()` 回调式逐段返回 | 与 ChatModel 同族，二选一 |
| `EmbeddingModel` | 把文本转成向量（`embed()`） | 供 RAG 用 |
| `ImageModel` / `AudioModel` | 生成图 / 转写音频 | 多模态专属 |
| `TokenCounter` | 统计 token 数（计成本） | 配合监控 |

**实现类关系**：如 `OpenAiChatModel`、`OllamaChatModel`、`GoogleAiGeminiChatModel` 都实现 `ChatModel` 接口 —— 所以代码可无缝换厂商。

### 2. 消息与提示词层

| 类 | 作用 |
|----|------|
| `SystemMessage` / `UserMessage` / `AiMessage` | 三类消息，构成对话上下文 |
| `PromptTemplate` | `{{变量}}` 模板，自动替换参数 |
| `StructuredPrompt` | 用 POJO 生成结构化提示词，参数自动映射字段 |

### 3. 数据解析/结构化输出层

| 类 | 作用 |
|----|------|
| `ChatResponse` | 一次模型回答的包装（含内容、token 用量等） |
| `output.structured.Description` | 给 POJO 字段加"说明"，帮 LLM 理解字段含义 |
| （隐式）返回类型转换 | AiService 依据方法返回类型，自动把文本解析成枚举/数字/日期/POJO/List |

### 4. 记忆层

| 类 | 作用 |
|----|------|
| `ChatMemory` | 对话记忆接口 |
| `MessageWindowChatMemory` | 滑动窗口记忆（最多 N 条） |
| `ChatMemoryProvider` | 按 `@MemoryId` 为每个用户/会话提供独立记忆 |
| 持久化实现 | MapDB 等把记忆落盘，重启不丢 |

### 5. AI 服务层（核心中的核心）

| 类/注解 | 作用 | 关系 |
|---------|------|------|
| `AiServices` | **工厂**：`create()`/`builder()` 生成接口实现 | 是"装配器" |
| `@SystemMessage` / `@UserMessage` | 声明系统提示词/用户消息模板 | 用在接口方法上 |
| `@V` | 把方法参数绑定到模板变量 | `@UserMessage("...{{x}}")` + `@V("x")` |
| `@MemoryId` | 标记"用户/会话标识"参数 | 配合 `chatMemoryProvider` |
| `@Tool` | 标记方法为"模型可调用的工具" | 用在工具类方法上（可注入 AiService） |
| `TokenStream` | 流式 AI 服务返回的流对象 | `StreamingChatModel` 配套 |

**关系图**：
```
AiServices.builder(Assistant.class)
    .chatModel(c)          // 装 模型
    .chatMemoryProvider(...)// 装 记忆
    .tools(toolInstance)   // 装 工具
    .contentRetriever(r)   // 装 检索(RAG)
    .build();              // 生成 Assistant 实例
```

### 6. RAG 层

| 类 | 作用 | 关系 |
|----|------|------|
| `Document` / `TextSegment` | 文档与切块后的文本段 | 是检索的基本单元 |
| `DocumentSplitter` | 把大文档切成小段 | 每个段独立向量化 |
| `EmbeddingStore` | 向量库（pgvector/Redis/内存等实现） | 存向量 |
| `EmbeddingStoreIngestor` | 一键完成"切块→向量化→入库" | 组装上面两者 |
| `ContentRetriever` | 检索器接口：根据问题取相关文本 | `EmbeddingStoreContentRetriever` 是被广泛使用的实现 |
| `RetrievalAugmentor` | 检索增强器：编排查询压缩/路由/重排等 | 装配到 AiService |
| `QueryRouter` / `Reranker` / `QueryTransformer` | 高级 RAG：路由、重排、查询压缩 | 挂在 RetrievalAugmentor 内 |

**RAG 数据流**：
```
文档 → DocumentLoader → DocumentSplitter 切块
     → EmbeddingModel 向量化 → EmbeddingStore 入库
  （运行期）问题 → ContentRetriever 检索 → 相关TextSegment
     → 拼接进提示词 → 交给 ChatModel → 基于资料回答
```

---

## 五、企业落地案例（10+ 个，含流程图）

> 以下每个案例都基于 AiService + 记忆/工具/RAG 的组合拳，是真实企业常见形态。流程图用文字框表示，`⟶` 表示数据流向。

### 案例 1：智能客服（个性路由 + 工具 + RAG + 持久记忆）
**场景**：企业客服系统，观众/租户各自独立会话，能查订单、能退款、能查售后政策。
```
用户提问
  └⟶ REST Controller
       └⟶ AiService.chat(@MemoryId 用户, 消息)
            ├⟶ ChatMemoryProvider 按用户隔离记忆
            ├⟶ ContentRetriever 检索 售后政策文档 (RAG)
            ├⟶ @Tool: 查订单 / 退款 / 改物流
            └⟶ ChatModel 组织回答
                 └⟶ 返回给用户 / 触发退款工单落库
```

### 案例 2：简历/JD 智能筛选（AI 信息抽取 + 结构化入库）
**场景**：HR 系统导入海量简历，自动抽出姓名/技能/年限/期望薪资入库，供检索。
```
上传简历(PDF)
  └⟶ DocumentLoader 解析文本
       └⟶ AiService `<Resume>从文本中抽取简历实体`
            ├⟶ 返回强类型 Resume POJO（姓名/技能/年限）
            └⟶ 可序列化 JSON 入库 / 发消息队列
                 └⟶ 后续按技能向量化，支持语义检索候选
```

### 案例 3：工单自动分诊（结构化输出 + 分类）
**场景**：B 端工单系统，把用户问题自动判定类别/紧急度/归属部门，并生成处理建议。
```
用户提交工单文本
  └⟶ AiService.analyze(工单)
       └⟶ 返回 {类别枚举, 紧急度, 归属团队, 建议动作}
            └⟶ 自动创建工单并派给对应团队 + 打标签
```

### 案例 4：企业知识库问答（多源 RAG + 重排）
**场景**：把企业内部手册/制度/FAQ 变成"能聊的同事"，答案带引用来源。
```
用户提问
  └⟶ QueryRouter 路由到正确知识源(订单/人力/财务…)
       └⟶ 多 ContentRetriever 并行检索
            └⟶ Reranker 重排取最相关
                 └⟶ 拼接上下文 + 给 ChatModel
                      └⟶ 回答 + 返回引用文档列表(ReturnSources)
```

### 案例 5：智能工单闭环 Agent（Agent 工作流：顺序 + 工具 + 人工审核）
**场景**：IT 报障从"受理→诊断→派单→（有人工确认）→解决"全自动/半自动流转。
```
用户报障
  └⟶ Agent1 受理:抽取问题&类别 (顺序工作流)
       └⟶ Agent2 诊断:检索知识库+查系统日志(工具)给出方案
            └⟶ 若非自动可解 ⟶ 主管 Agent (Supervisor) 派单给工程师
            └⟶ Human-in-the-loop: 关键操作等人工确认
                 └⟶ 解决后 Agent3 汇总并回执给用户
```

### 案例 6：舆情/评论情感监控（Embedding 分类 + 定时任务）
**场景**：某平台监控用户评论情感（正面/负面），负面自动报警。
```
评论流
  └⟶ EmbeddingModel 向量化 → 与情感样本库比对
       └⟶ 或 AiService 判断情感枚举(正面/中性/负面)
            └⟶ 负面 → 触发告警/生成跟进工单
```

### 案例 7：多语言内容翻译与本地化（提示词模板 + AI 服务）
**场景**：产品文案/合同自动翻译并保持风格，生成多语言版本。
```
原文 + 目标语言
  └⟶ AiService.translate(text, language)
       └⟶ @SystemMessage("你是专业{{language}}翻译") + 风格约束
            └⟶ 返回目标语言文本 → 存入 CMS
```

### 案例 8：SQL / 报表智能生成（Text-to-SQL + RAG）
**场景**：业务人员用自然语言问"上月各区域销售额"，系统转成 SQL 查库返回。
```
自然语言问题
  └⟶ Async/RAG: 检索目标表结构(约束) 拼接进提示词
       └⟶ AiService 生成 SQL → 校验(沙箱/人工确认)
            └⟶ 执行查询 → 结果经 AiService 转成自然语言摘要
```

### 案例 9：会议纪要与行动项提取（文档处理 + 结构化输出）
**场景**：把会议录音转写文本，自动生成结构化纪要并提取待办。
```
转写文本
  └⟶ DocumentLoader 读取
       └⟶ AiService 产出 MeetingNotes POJO
            └⟶ {主题, 决议, 行动项列表[责任人,截止] , 风险}
                 └⟶ 自动生成到项目管理系统 / 发通知
```

### 案例 10：合规审查（多 Agent 并行 + 汇总 + 人工复核）
**场景**：合同/营销文案并行过"法律"“品牌""数据隐私"多个审核 Agent，聚合结果。
```
待审文本
  └⟶ 并行: Agent法律 / Agent品牌 / Agent隐私 (并行工作流)
       ├⟶ 各自返回 {是否通过, 风险点}
       └⟶ 聚合 Agent 汇总风险清单
            └⟶ 高风险 → 人工复核 (Human-in-the-loop) → 输出合规结论
```

### 案例 11：智能搜索增强（Query 压缩 + 混合检索）
**场景**：电商/文档站的搜索，先压缩多轮问题再检索，支持跨库混合。
```
用户多轮追问
  └⟶ QueryCompression 压缩成独立问题
       └⟶ 混合检索(向量库 + 全文检索) → 融合排序
            └⟶ 返回相关片段 + 原文链接
```

### 案例 12：Agent 接入外部服务（MCP 工具）
**场景**：让 Agent 通过 MCP 直接操作企业外/内的第三方服务（如订票、GitHub、CRM）。
```
用户意图"在新系统里给张三创建订单"
  └⟶ AiService(Agent) 决定用哪个工具
       └⟶ 通过 MCP(MCP Server) 调用 CRM 创建订单工具
            └⟶ 返回操作结果 → Agent 组织话术回复用户
```

### 案例 13：模型路由与降级（多厂商 + 成本优化）
**场景**：简单问题用便宜小模型，复杂问题用大模型；某厂商故障自动切换。
```
请求
  └⟶ 路由策略(问题复杂度) 
       ├⟶ 简单 → ChatModel(便宜小模型)
       └⟶ 复杂/失败 → 降级 ChatModel(大模型/备用厂商)
            └⟶ 统一经 AiService 返回，调用方无感知
```

### 案例 14：流式聊天应用（SSE 流式 + 前端打字机）
**场景**：面向用户的 AI 聊天产品（可接入前端流式展示）。
```
前端消息
  └⟶ Controller 返回 SSE 流
       └⟶ StreamingChatModel 逐段推送 → 浏览器打字机展示
            └⟶ 消息同时写入 ChatMemory 积累上下文
```

---

## 六、给你的一条路径（如何用这份总览）

1. **先用这份总览**建立整体地图——知道每个能力在哪、类之间什么关系。
2. **再进各模块的学习指导.md**，按文件优先级逐个跑。
3. **带着"企业案例"去学**：每学一个能力（如 RAG、工具、记忆），回来看它对应当标案例，理解"这个能力在真实产品里解决什么问题"。
4. 面试时：能讲清 **AiService + 四个组件（记忆/工具/检索/模型）如何组装**，再结合 2~3 个你亲手跑的案例（如客服、简历抽取、知识库），就是一份有说服力的 AI 开发能力证明。

> 建议从 `tutorials` 入门 → `customer-support-agent-example` 看完整闭环 → `agentic-tutorial` 学编排 → `rag-examples` 学检索 → `spring-boot-example` 学生产落地。这条路走完，LangChain4j 的核心能力就都掌握了。